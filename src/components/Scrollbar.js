import { Scrollbars } from 'react-custom-scrollbars';

const Scrollbar = ({ id, titulo }) => {
        return (
            <Scrollbars
                style={{ height: 300 }}>
                <Link><li></li></Link>
            </Scrollbars>
        );

    }

    export default Scrollbar;
    